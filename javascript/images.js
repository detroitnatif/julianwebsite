document.addEventListener('DOMContentLoaded', function() {
    // Create a new image object
    var img = new Image();
    // Set the source of the image
    img.src = '../imgs/trunks.png';

    var img2 = new Image();
    // Set the source of the image
    img.src = '../imgs/pokemontrainer.webp';
});
